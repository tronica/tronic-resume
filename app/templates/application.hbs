{{#if showLoadingView}}
  {{loading-overlay loadingMessages=loadingMessages}}
{{/if}}

{{#if showNavBar}}
  {{nav-bar}}
{{/if}}

{{outlet}}

  {{confirm-modal
      title=(t 'form.modal.confirm_delete_head')
      body=(t 'form.modal.confirm_delete_body')
  }}